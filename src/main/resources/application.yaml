logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  datasource:
    url:  jdbc:h2:mem:boagliomvp
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
   show-sql: true
   hibernate:
        ddl-auto: none
management:
  security:
    enabled: false